<%= form_with(model: workshop, local: true) do |form| %>
  <% if workshop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workshop.errors.count, "error") %> prohibited this workshop from being saved:</h2>

      <ul>
      <% workshop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre_Taller %>
    <%= form.text_field :first_title, id: :workshop_first_title, :class => "form-control" %>
  </div>

  <div class="field">
    <%= form.label :Descripcion_Taller %>
    <%= form.text_area :first_description, id: :workshop_first_description, :class => "form-control" %>
  </div>

   <div class="field">
        <%= form.label :"imagen1" %><br />
        <%= form.file_field :avatar %>
    </div>

  <div class="actions">
    <%= form.submit :type => "submit", :class => "btn btn-default" %>
  </div>
<% end %>
